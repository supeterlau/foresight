# Frontend build-time env vars (VITE_* are inlined by Vite at build time)

# API base URL for production (Render backend service URL)
# In dev mode, Vite proxy handles this automatically
VITE_API_URL="http://localhost:8080"

# Supported language codes (comma separated)
LANGUAGES="zh,en"
